name: ginseng_farm_app
description: 다함께 차차차 - 함께 쓰는 AI영농일지
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  http: ^1.2.1
  image_picker: ^1.1.2
  image_gallery_saver_plus: ^3.0.5
  intl: ^0.19.0
  provider: ^6.1.2
  table_calendar: ^3.1.2
  fl_chart: ^0.68.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.3
  speech_to_text: ^6.6.2
  flutter_tts: ^3.8.5
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
